{
    "version": 2,
    "name": "carlosbarçante",
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node",
            "config": {
                "env": {
                    "NODE_ENV": "production"
                },
                "command": "npm run build && npm run start"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server.js"
        }
    ]
}